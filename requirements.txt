# Core LLM + RAG
# NOTE: pinned to the 0.2.x line deliberately. LangChain 1.x removed
# langchain.chains / langchain.memory (legacy chain API) in favour of
# LangGraph-style agents. This project's ConversationalRetrievalChain
# pattern requires 0.2.x.
langchain==0.2.16
langchain-community==0.2.16
langchain-openai==0.1.23
langchain-core==0.2.38
llama-index>=0.10.0
llama-index-core>=0.10.0

# Vector stores
faiss-cpu>=1.7.4
chromadb>=0.5.0

# Document parsing
pypdf>=4.0.0
python-docx>=1.1.0

# Embeddings
sentence-transformers>=3.0.0
tiktoken>=0.7.0
openai>=1.30.0

# Vision AI (Phase 3)
boto3>=1.34.0                  # AWS Rekognition + SageMaker
google-cloud-vision>=3.7.0     # GCP Vision API

# URL ingestion (Phase 3)
requests>=2.31.0
beautifulsoup4>=4.12.0

# API + deployment
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # for file uploads in FastAPI
sse-starlette>=2.1.0           # Server-Sent Events streaming
streamlit>=1.35.0

# Testing
pytest>=8.0.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.7.0
